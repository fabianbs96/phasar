import lit.formats
import os

config.name = "IDE taint test"
config.test_format = lit.formats.ShTest(execute_external=False)
config.suffixes = ['.c', '.cpp']

config.test_paths = ['double_free']

config.test_source_root = os.path.dirname(__file__)
